##############################################################
#  Financial RAG Defense — Environment Variables Template
#  Copy this file to .env and fill in your keys.
#  NEVER commit .env to git.
##############################################################

# ─── FREE TIER PROVIDERS ──────────────────────────────────
# Groq (14,400 req/day free) — primary RAG + attacker LLM
# Sign up at: https://console.groq.com
GROQ_API_KEY=your_groq_key_here

# Google AI Studio / Gemini (1,500 req/day free) — judge + filtering
# Get key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_key_here

# OpenRouter (200 req/day free — fallback only)
# Sign up at: https://openrouter.ai
OPENROUTER_API_KEY=your_openrouter_key_here

# ─── OPTIONAL ─────────────────────────────────────────────
# HuggingFace token — only needed if using local models or
# gated models (Llama Prompt Guard). Can leave empty.
HF_TOKEN=

# ─── MODEL OVERRIDES (optional — defaults set in config.py) ─
# BASE_LLM_PROVIDER=groq
# BASE_LLM_MODEL=llama-3.1-8b-instant
# JUDGE_LLM_PROVIDER=gemini
# JUDGE_LLM_MODEL=gemini-2.0-flash
